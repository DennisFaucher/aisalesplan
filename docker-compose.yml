services:
  web:
    build: .
    ports:
      - "5001:5000"
    environment:
      - PERPLEXITY_API_KEY=${PERPLEXITY_API_KEY:your_key_goes_here}
    volumes:
      - .:/app
    restart: unless-stopped

